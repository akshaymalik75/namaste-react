const heading = React.createElement(
  "h4",
  {},
  "Hello akshay malik this is coming from react"
);

const root = ReactDOM.createRoot(document.getElementById("root"));
const abc = root.render(heading);

console.log(abc);
